import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
    display: flex;
    width: 50%;
    background: rgb(5,5,5);
    background: linear-gradient(180deg, rgba(5,5,5,1) -17%, rgba(40,5,73,1) 100%);
    border-radius: 10px;
`

const Panel = () => {
    return (
        <div>
            
        </div>
    )
};

export default Panel;

